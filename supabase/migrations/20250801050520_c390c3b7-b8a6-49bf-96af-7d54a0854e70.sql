-- This migration adds the RESEND_API_KEY to Supabase secrets
-- The actual secret value will be set through the Supabase dashboard
SELECT 1; -- Placeholder query since we can't directly set secrets via SQL